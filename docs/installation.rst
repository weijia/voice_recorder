============
Installation
============

At the command line::

    $ easy_install voice_recorder

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv voice_recorder
    $ pip install voice_recorder